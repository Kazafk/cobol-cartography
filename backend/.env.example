# Cobol Cartography backend — copy to .env and adjust
AI_MODE=local_strict
ALLOW_EXTERNAL_AI=false
LOG_LEVEL=INFO
PARSER_JAR_PATH=
GRAPH_BACKEND=sqlite
# Override to 0.0.0.0 in Docker/container environments
HOST=127.0.0.1
PORT=8000
